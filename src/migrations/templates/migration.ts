import type { Surreal } from "surrealdb";

export async function up(db: Surreal): Promise<void> {
  // await db.query(`...`);
}

export async function down(db: Surreal): Promise<void> {
  // await db.query(`...`);
}
